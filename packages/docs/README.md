# platformd 运维文档

VitePress 站点，面向安装和运行 `platformd` 的人，不是贡献者。中文是默认语言（`/`，`zh-CN`），英文在 `/en/`（`en-US`）。导航里有语言切换。

从仓库根目录：

```sh
bun run docs:dev
bun run docs:build
bun run docs:preview
```

也可以在本目录运行 `bun run dev` / `build` / `preview`。

生成输出在 `.vitepress/dist/`，缓存在 `.vitepress/cache/`，二者都不提交。
