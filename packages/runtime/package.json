{
  "name": "@open-compute/runtime",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://open-compute.dev",
  "type": "module",
  "scripts": {
    "build": "bun build.ts",
    "check:generated": "bun build.ts --check",
    "test": "node --test 'tests/**/*.test.mjs'",
    "typecheck": "tsc --project tsconfig.json --noEmit && tsc --project tsconfig.build.json --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@types/node": "catalog:",
    "rolldown": "catalog:"
  }
}
